using System;

namespace HelloWorld
{
    class HelloWorld
    {
        static void Main()
        {
            string hello = "Hello";
            string world = "World";
            object helloWorld = hello + " " + world;
            Console.WriteLine(helloWorld);

            string finalResult = (string)helloWorld;
            Console.WriteLine(finalResult);
        }
    }
}
